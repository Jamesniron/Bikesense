export const environment = {
  production: true,
  // Relative path: backend serves this frontend and the API from the same origin.
  apiUrl: '/api',
  mlServiceUrl: 'https://redundant-klarrisa-nithujanzee-0998d30f.koyeb.app'
};
